{
  "name": "todolistserver",
  "version": "1.0.0",
  "description": "custom json-api server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "adhikarirahul.com.np",
  "license": "ISC",
  "dependencies": {
    "json-server": "0.17.4"
  }
}
